# Copyright 2025 Antmicro
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

"""VC Static rules"""

load("@rules_hdl//common:providers.bzl", "LogInfo")
load("@rules_hdl//synthesis:defs.bzl", "SdcInfo")
load("@rules_hdl//verilog:defs.bzl", "VerilogInfo")

def _value_to_tcl(value):
    if type(value) == "string":
        return "\"{}\"".format(value)
    elif type(value) == "int":
        return value
    elif type(value) == "bool":
        return str(value).lower()
    fail("Unsupported type convertion to TCL: {}".format(type(value)))

def _dict_to_tcl(dictionary):
    tcl = []
    for name, value in dictionary.items():
        definition = ["set", name]
        if type(value) == "list":
            definition.extend([_value_to_tcl(" ".join(value))])
        else:
            definition.append(_value_to_tcl(value))
        tcl.append(" ".join(definition))

    return "\n".join(tcl)

def _vc_static_lint(ctx):
    transitive_srcs = depset([], transitive = [ctx.attr.module[VerilogInfo].dag]).to_list()

    # Get flatten sources and headers
    all_srcs = [verilog_info_struct.srcs for verilog_info_struct in transitive_srcs]
    all_hdrs = [verilog_info_struct.hdrs for verilog_info_struct in transitive_srcs]

    all_srcs = [src for sub_tuple in all_srcs for src in sub_tuple]
    all_hdrs = [hdr for sub_tuple in all_hdrs for hdr in sub_tuple]

    log_file = ctx.actions.declare_file("{}.log".format(ctx.label.name))
    report_file = ctx.actions.declare_file("{}_report.txt".format(ctx.label.name))
    outputs = [log_file, report_file]
    tcl_script = ctx.actions.declare_file("{}.tcl".format(ctx.label.name))
    outputs = [log_file, report_file]

    if ctx.attr.goal_name == "rdc" or ctx.attr.goal_name == "cdc":
        # log_file = ctx.actions.declare_file("{}_{}.log".format(ctx.label.name, ctx.attr.goal_name))
        # report_file = ctx.actions.declare_file("{}_{}_report.txt".format(ctx.label.name, ctx.attr.goal_name))
        # tcl_script = ctx.actions.declare_file("{}_{}.tcl".format(ctx.label.name, ctx.attr.goal_name))
        sdc_file_list = ctx.attr.sdc_file[SdcInfo].sdc.files.to_list()
        if len(sdc_file_list) > 1:
            fail("There can be only one sdc file for this target")
        sdc_variables = {
            "report_file": report_file.path,
            "sdc_file": sdc_file_list[0].path if len(sdc_file_list) else "",
        }

    tcl_variables = {
        "all_severities": ctx.attr.severities,
        "config_files": [f.path for f in ctx.files.config_files],
        "enable_liberty": ctx.attr.enable_liberty,
        "failing_severities": ctx.attr.failing_severities,
        "goal_name": ctx.attr.goal_name,
        "include_dirs": depset([f.dirname for f in (all_srcs + all_hdrs)]).to_list(),
        "report_file": report_file.path,
        "sources": [f.path for f in all_srcs],
        "top_module": ctx.attr.module_top,
        "vcs_opts": ctx.attr.vcs_opts,
        "waiver_files": [f.path for f in ctx.files.waiver_files],
    }
    inputs = [ctx.file.vc_static_env, tcl_script] + \
             ctx.files.waiver_files + \
             ctx.files.config_files + \
             all_hdrs + \
             all_srcs

    # Write the TCL script
    if ctx.attr.goal_name == "rdc" or ctx.attr.goal_name == "cdc":
        subs = {
            "#{{AUTOGENERATED_SCRIPT}}#": _dict_to_tcl(tcl_variables),
            "#{{SDC_AUTOGENERATED_VARS}}#": _dict_to_tcl(sdc_variables),  # buildifier: disable=uninitialized
        }
        inputs += sdc_file_list  # buildifier: disable=uninitialized

    else:
        subs = {
            "#{{AUTOGENERATED_SCRIPT}}#": _dict_to_tcl(tcl_variables),
        }
    ctx.actions.expand_template(
        template = ctx.file.tcl_script,
        output = tcl_script,
        substitutions = subs,
    )
    vc_command = [
        "source",
        ctx.file.vc_static_env.path,
        "&&",
        "vc_static_shell",
        "-no_ui",
        "-no_restore",
        "-batch",
        "-output_log_file",
        log_file.path,
        "-file",
        tcl_script.path,
    ] + ctx.attr.opts

    ctx.actions.run_shell(
        outputs = outputs,
        inputs = inputs,
        progress_message = "Running VC Static: {}".format(ctx.label.name),
        command = " ".join(vc_command),
    )

    return [
        DefaultInfo(
            files = depset(outputs),
            runfiles = ctx.runfiles(files = []),
        ),
        LogInfo(
            files = [log_file],
        ),
    ]

vc_static_lint = rule(
    implementation = _vc_static_lint,
    attrs = {
        "cdc_script": attr.label(
            doc = "A TCL script run by VC Static to check RDC compliance",
            default = "//vc_static:cdc.tcl",
            allow_single_file = True,
        ),
        "config_files": attr.label_list(
            doc = "Config files",
            allow_files = True,
        ),
        "enable_liberty": attr.bool(
            doc = "Enable liberty database load",
            default = False,
        ),
        "failing_severities": attr.string_list(
            doc = "List of lint severity names to fail on",
            default = ["fatal", "error", "warning"],
        ),
        "goal_name": attr.string(
            doc = "Lint goal name",
            default = "lint_rtl",
        ),
        "module": attr.label(
            doc = "The top level module target to lint.",
            providers = [VerilogInfo],
            mandatory = True,
        ),
        "module_top": attr.string(
            doc = "The name of the top-level module in the design.",
            mandatory = True,
        ),
        "opts": attr.string_list(
            doc = "Additional command line options to pass to vc_static",
            default = [
                "-no_init",
                "-mode64",
                "-lic_wait",
                "1140",
            ],
        ),
        "rdc_script": attr.label(
            doc = "A TCL script run by VC Static to check RDC compliance",
            default = "//vc_static:rdc.tcl",
            allow_single_file = True,
        ),
        "sdc_file": attr.label(
            doc = "WIP",
            allow_single_file = True,
        ),
        "severities": attr.string_list(
            doc = "List of lint severity names to report",
            default = ["fatal", "error", "warning", "info"],
        ),
        "tcl_script": attr.label(
            doc = "A TCL script run by VC Static",
            default = "//vc_static:default.tcl",
            allow_single_file = True,
        ),
        "vc_static_env": attr.label(
            doc = "A shell script to source the vc_static environment and " +
                  "point to license server",
            mandatory = True,
            allow_single_file = [".sh"],
        ),
        "vcs_opts": attr.string(
            doc = "Additional option to pass to vc-static 'analyze -vcs' command.",
        ),
        "waiver_files": attr.label_list(
            doc = "Waiver files",
            allow_files = True,
        ),
    },
    provides = [
        DefaultInfo,
        LogInfo,
    ],
)
